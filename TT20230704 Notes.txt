Loaded libraries to use
 - First time using the mapview library

Loaded dataset as 'historical_markers' and applied glimpse(). 

Using the mapview library from earlier, plotted the location of all the markers onto a map.

Ordered the 50 states and Puerto Rico from most to least markers.

Used sqldf functions to order the cities/towns by their total number of markers. Also attached data for the city/town's state.

Ordered the data by the organizations that erected the most markers from most to least.